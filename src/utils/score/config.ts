/**
 * 合格率計算に関する設定値を定義するファイル
 * 
 * @description 合格率計算に必要な各種パラメータとデフォルト値を管理します。
 * これにより、計算ロジックと設定値を分離し、将来的な調整を容易にします。
 */

/**
 * 合格率計算の設定オブジェクト
 */
export const ScoreConfig = {
  /**
   * 必須問題1問を落とした場合のペナルティ係数
   * 例: 0.2は20%減点を意味する
   */
  MANDATORY_PENALTY_FACTOR: 0.2,
  
  /**
   * 難易度別の加点係数
   * 各難易度の問題を正解した際に加算されるボーナス値（%単位）
   */
  DIFFICULTY_BONUS: {
    LOW: 0, // 低難易度は加点なし
    MID: 0, // 中難易度は加点なし
    HIGH: 0.2, // 高難易度は1問あたり0.2%加点
  },
  
  /**
   * 合格率の最大値（％）
   * trueの場合、計算結果がこの値を超えた場合はこの値に丸められる
   */
  MAX_SCORE: 100,
  
  /**
   * 最大値を適用するかどうか
   * falseの場合、計算結果がMAX_SCOREを超えても丸めない
   */
  CLIP_MAX_SCORE: true,
  
  /**
   * 小数点以下の桁数
   * 計算結果の丸め桁数（小数点以下何桁まで表示するか）
   */
  DECIMAL_PLACES: 1,
}; 